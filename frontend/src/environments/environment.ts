// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// Use this when running back-end on Heroku!

export const environment = {
    production: false,
    baseUrl: 'https://back-end-test-for-heroku.herokuapp.com'
};
